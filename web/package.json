{
  "name": "power-monitor-sensor-agent-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "format": "prettier --write src tests *.ts *.html",
    "format:check": "prettier --check src tests *.ts *.html"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "jsdom": "29.1.1",
    "prettier": "3.8.1",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "4.1.1"
  }
}
